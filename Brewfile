# Brewfile - Homebrew dependencies for development environment
#
# Install with: brew bundle --file=~/repos/dotfiles/Brewfile
# Check status: brew bundle check --file=~/repos/dotfiles/Brewfile
#
# See docs/package-management.md for strategy and rationale.

# Taps
# (homebrew/bundle is now built-in, no tap needed)

# =============================================================================
# Core Development Tools
# =============================================================================

brew "git"                    # Version control

# -----------------------------------------------------------------------------
# Security & Secret Management
# -----------------------------------------------------------------------------

brew "pre-commit"             # Git hook framework for code quality checks
brew "gitleaks"               # Scans git repos for secrets/credentials

# -----------------------------------------------------------------------------
# Shell
# -----------------------------------------------------------------------------

brew "bash"                   # Updated bash (macOS ships with old 3.x version)

# -----------------------------------------------------------------------------
# Languages & Runtimes
# -----------------------------------------------------------------------------

brew "python"                 # Python 3
brew "node"                   # Node.js / JavaScript runtime

# -----------------------------------------------------------------------------
# Hardware / Serial
# -----------------------------------------------------------------------------

brew "minicom"                # Serial port terminal (for homelab console access)

# =============================================================================
# Future Additions (uncomment as needed)
# =============================================================================

# JSON/YAML Processing
# brew "jq"                   # JSON processor
# brew "yq"                   # YAML processor

# Cloud & Infrastructure
brew "gh"                     # GitHub CLI
# brew "ansible"              # Configuration management
# brew "terraform"            # Infrastructure as code

# Containers
# brew "docker"               # Container runtime (or use Docker Desktop cask)
